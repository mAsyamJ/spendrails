# @spendrail/dashboard

Agent-readable JSON API plus SSE events and a minimal HTML view.

```bash
npx spendrail-dashboard
```

Environment: `SPENDRAIL_DASHBOARD_PORT`, `SPENDRAIL_DASHBOARD_HOST`,
`SPENDRAIL_DASHBOARD_TOKEN`.
